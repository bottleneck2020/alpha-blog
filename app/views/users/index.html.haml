%h1{:align => "center"} The users index page
.center
  = will_paginate
  - @users.each do |user|
    %ul.listing
      .row
        .well.col-md-12.col-md-offset-12
          %li= link_to gravatar_for(user), user_path(user)
          %li.article-title
            = link_to user.username, user_path(user)
          %li
            %small= pluralize(user.articles.count,"article") if user.articles
  = will_paginate
